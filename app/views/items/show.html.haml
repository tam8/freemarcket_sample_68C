.itemsShow
  %main.main
    .main__name
      =@item.name
    .main__imgs
      =image_tag @item_imgs[0].image_url, class: "main__imgs__img"

      %ul.main__imgs__list
        -@item_imgs.each do |i|
          %li.main__imgs__list__img
            =image_tag i.image_url, class: ""

    %p.main__price
      \\
      =@item.price
      %p.main__price__sub
        (税込) 送料込み
    
    -if user_signed_in? && current_user.id != @item.user_id
      =link_to "#", class:"main__buy" do
        購入画面に進む
    
    -if user_signed_in? && current_user.id == @item.user_id
      =link_to "#", class:"main__edit" do
        商品の編集

      =link_to "#", class:"main__destroy" do
        商品の削除


    %p.main__description
      =@item.explain

    %table.main__info
      %tbody
        %tr
          %th
            商品の状態
          %td
            =@item.status
        %tr
          %th
            ブランド
          %td
            %a
              =@item.brand
        %tr
          %th
            配送料の負担
          %td
            =@item.shipping_fee
        %tr
          %th
            配送方法
          %td
            =@item.shipping_method
        %tr
          %th
            発送元の地域
          %td
            =@item.owners_area
        %tr
          %th
            発送日の目安
          %td
            =@item.arrival_date
        %tr
          %th
            出品者
          %td
            =User.find(@item.user_id).nickname
        %tr
          %th
            カテゴリー
          %td
            %a
              =Category.find(@item.category_id).name

    .main__low
      %a.main__low--like
        \★ お気に入り 3
      %a.main__low--report
        =icon("fa", "flag")
        不適切な商品の通報

  %form.comment
    %textarea.comment__txt
    .comment__caution
      相手のことを考え丁寧なコメントを心がけましょう。<br>
      不快な言葉遣いなどは利用制限や退会処分となることがあります。
    %button.comment__btn
      =icon("fas", "comment")
      コメントする
  
  .more
    .preNextBox
      =link_to "< 前の商品", "#", class: "preBtn"
      =link_to "#", class: "nextBtn" do
        -# =params[:id].to_i - 1
        -# = "#{item_path}" + "#{params[:id].to_i - 1}"
        次の商品 >
    =link_to "#{Category.find(@item.category_id).name}をもっと見る", "#", class:"moreBtn"





















